

class poloniex_emc2_bot:

    def get_account_info():
    #     retrieve balance, orders, etc

    def predict_epoch(mdf, x, y):
        phigh = mdf.iloc[day-1]['high']
        plow = mdf.iloc[day-1]['low']
        return high, low

    def buy_sell_hold(mdf, day, phigh, plow):
        return action

    def act(action):
    #     poloniex api actions. Buy, sell, etc

    def paper_test(mdf, period):

        for epoch in range(1, mdf.len):
            tmdf
            phigh, plow = predict_day(mdf, epoch)
            action = buy_sell_hold(high, low)
